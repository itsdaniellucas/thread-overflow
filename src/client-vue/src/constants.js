
export const thread = Object.freeze({
    self: 'self',
    all: 'all',
    thread: 'thread',
    message: 'message',
})

export const defaultPagination = Object.freeze({
    page: 0,
    itemsPerPage: 10,
    totalPages: 5,
})